# Personal dotfiles
Just clone and use stow to populate the config. 

```bash
stow config
```

## Nvim config is configured to be a submodule and must be fetched with
```bash
git submodule update --init --recursive
```
