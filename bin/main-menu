#!/bin/bash

declare -A OPTIONS=(
  ["  Reboot"]="systemctl reboot"
  ["  Shutdown"]="systemctl poweroff"
  ["󱧕  Install package"]="launch-terminal install-package"
  ["󱧔  Uninstall package"]="launch-terminal uninstall-package"
  ["󰂯  Bluetooth"]="launch-terminal bluetui"
  ["  Github"]="chromium https://github.com/SmartEVP8"
  ["󰍳  Minecraft music"]="chromium https://www.youtube.com/watch?v=yJ6Lbsmb1lY"
)

# Create the menu list
MENU=$(printf "%s\n" "${!OPTIONS[@]}")

# Show menu

# SELECTION=$(echo -e "$MENU" | walker --dmenu --width 500 --minheight 1 -p "")
SELECTION=$(echo -e "$MENU" | vicinae dmenu --width 600 -p "")

# Execute mapped command
[[ -n "$SELECTION" ]] && ${OPTIONS["$SELECTION"]}
